[common]
parnames = [color, layout, font_size, background]
lb = [0, 0, 0, 0]
ub = [4, 3, 5, 2]
stimuli_per_trial = 1
outcome_types = [binary]
strategy_names = [init_strat, opt_strat]

[color]
par_type = categorical
choices = [red, orange, yellow, green, blue]

[layout]
par_type = categorical
choices = [list, grid, table, card]

[font_size]
par_type = categorical
choices = [Font=10, Font=11, Font=12, Font=13, Font=14, Font=15]

[background]
par_type = categorical
choices = [white, gray, none]

[init_strat]
generator = SobolGenerator
min_asks = 20

[opt_strat]
generator = OptimizeAcqfGenerator
acqf = HardExclusionAcqf
model = GPClassificationModel
min_asks = 60

[GPClassificationModel]
inducing_size = 50
mean_covar_factory = default_mean_covar_factory

[HardExclusionAcqf]
# V3方案A配置: 使用V1的全部参数 + 自动硬排除
# 交互效应权重范围 (动态: lambda_min -> lambda_max)
lambda_min = 0.5
lambda_max = 3.0

# 方差阈值
tau_1 = 0.5
tau_2 = 0.3

# 覆盖度权重 (固定，不动态调整)
gamma = 0.5

# 覆盖度计算方法
coverage_method = min_distance

# 交互项定义 (根据先验知识或全交互)
interaction_terms = (0,1);(0,2);(0,3);(1,2);(1,3);(2,3)

# 注意: V3不需要额外参数，硬排除自动生效
